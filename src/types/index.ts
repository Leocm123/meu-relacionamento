export interface TimeState {
  years?: number;
  months?: number;
  days?: number;
  totalDays?: number;
}

export interface TimelineItem {
  date: string;
  title: string;
  desc: string;
  img: string;
}
